declare module 'country-iso-3-to-2' {
    // Add type definitions if available, or leave it as a module declaration
    function getCountryISO2(countryISO3: string): string;
    export = getCountryISO2;
}